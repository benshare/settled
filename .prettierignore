# Regenerated by `npm run types` — prettier and the Supabase generator disagree
# on quote style, so formatting it churns ~900 lines on every regen.
lib/database-types.ts

# Lockfile — pnpm owns its formatting; running prettier over it rewrites
# ~18k lines and the next `pnpm install` reverts them.
pnpm-lock.yaml
